<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="id.ac.umn.warehaus">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission
        android:name="android.permission.LOCATION_HARDWARE.Camera"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/warehouselogo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/warehouselogo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MenuActivity" />
        <activity
            android:name=".HistoryActivity"
            android:label="History" />
        <activity android:name=".AddInventoryActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".ShipmentActivity"></activity>
    </application>

</manifest>